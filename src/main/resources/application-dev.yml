spring:
  datasource:
    url: jdbc:postgresql://localhost:6789/testdb
    username: postgres
    password: test
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
